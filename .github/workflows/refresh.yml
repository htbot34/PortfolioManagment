name: Refresh portfolio site

on:
  schedule:
    # Weekdays at 11:00 UTC = 7am ET (EDT) / 6am ET (EST) - before market open
    - cron: "0 11 * * 1-5"
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - "portfolio.yaml"
      - "risk_profile.yaml"
      - "app/**"
      - "requirements.txt"
      - ".github/workflows/refresh.yml"

permissions:
  contents: write
  models: read

concurrency:
  group: refresh
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Run tests
        run: pytest -q

      - name: Build site
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_MODEL_ROUTINE: openai/gpt-4o-mini
          GITHUB_MODEL_SYNTHESIS: openai/gpt-4o
          LLM_MIN_GAP_S: "8"
          SEC_USER_AGENT: PortfolioAdvisor ${{ github.repository_owner }}@users.noreply.github.com
        run: python -m app.build_site

      - name: Commit built site
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html positions.html scanner.html recommendations.html candidates.html data.json ticker .nojekyll 2>/dev/null || true
          if git diff --staged --quiet; then
            echo "No site changes."
          else
            git commit -m "Refresh site $(date -u +%Y-%m-%dT%H:%MZ)"
            git push
          fi
